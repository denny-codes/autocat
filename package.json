{
  "name": "autocat",
  "version": "0.1.0",
  "description": "Categorize a Chrome bookmark folder with Claude. Local web app at localhost:3000.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "cheerio": "^1.0.0",
    "express": "^4.21.0",
    "multer": "^1.4.5-lts.1"
  }
}
